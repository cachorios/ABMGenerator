{% block extends %}
    {{ "{% extends '"~ plantillas.layout_gral ~ "' %}" }}
{% endblock extends %}
    {{ "{% block id '" ~entity~ "' %} " }}
    {% block body %}


        {{ "{% block " ~ plantillas.block_content ~ " -%}" }}
        {% block form_inicio %}
            {{ '   {{ form_start(form) }} ' }}
        {% endblock form_inicio %}
        {{ "{% embed '"~ plantillas.box_crud ~"' with {'titulo': 'Nuevo " ~  entity  ~ "' } %}" }}

        {#{{ "    {% block in_header %} " }}#}
        {#{% set hide_edit, hide_delete = true, true %}#}
        {#<div class='pull-right'>#}
        {#<div class="row">#}
        {#{% include 'crud/views/others/record_actions.html.twig' %}#}
        {#</div>#}
        {#</div>#}
        {#{{ "    {% endblock %}" }}#}

        {{ "      {% block rb_contenido %} " }}
        {{ "        {% for type, flashMessages in app.session.flashbag.all() %} " }}
        {{ "            {% for flashMessage in flashMessages %} " }}
        {{ "                <div class='alert alert-{{ type }}'> " }}
        <button data-dismiss="alert" class="close">
            ×
        </button>
        {{ "                    {{ flashMessage }} " }}
        {{ "                </div> " }}
        {{ "            {% endfor %} " }}
        {{ "        {% endfor %}" }}


        {{ '        {{ form_widget(form) }} ' }}

        {#{{ '        <div class="col-sm-offset-2 inner-actions">#}
                    {#<button title="Guardar"         class="form-submit-button btn btn-xs btn-info " value="save_and_add" name="save_mode" type="submit"><span class="fa fa-floppy-o"></span>&nbsp;Guardar y Nuevo </span></button>#}
                    {#<button title="Guardar y Salir" class="form-submit-button btn btn-xs btn-primary" value="save_and_close" name="save_mode" type="submit"><span class="fa fa-floppy-o"></span>&nbsp;Guardar</button>#}
                {#</div> ' }}#}

        {{ "{% endblock rb_contenido %} " }}


        {{ "    {% block rb_widget_header_buttons %} " }}

        {% set etiqueta, icono, clase = "", "","" %}
        <ul class="rb-toolbar pull-right">

            {% for key, accion in new_cfg.botones_arriba %}
            <li>
            {% set etiqueta, icono, clase = accion.label, accion.icon,accion.class %}
            {% include "crud/views/others/new_"~key~".html.twig"  %}
            </li>
            {% endfor %}

            <li>
                {{ "       {{ parent() }} " }}
            </li>

        </ul>

        {{ "{% endblock %} " }}

        {{ "    {% block rb_widget_footer_buttons %} " }}
        <ul class="rb-toolbar pull-right">

            {% for key, accion in new_cfg.botones_abajo %}
            <li>
            {% set etiqueta = accion.label %}
            {% set etiqueta, icono, clase = accion.label, accion.icon,accion.class %}
            {% include "crud/views/others/new_"~key~".html.twig"  %}
            </li>
            {% endfor %}

            <li class="tool-space-rigth">

            </li>
        </ul>
        {% block form_fin %}
            {{ '  {{ form_end(form) }}  ' }}
        {% endblock form_fin %}
        {{ "    {% endblock %} " }}



        {{ "{% endembed %} " }}

        {{ "{% endblock " ~ plantillas.block_content ~ " -%}" }}
    {% endblock body %}
