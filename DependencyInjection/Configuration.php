<?php

namespace RBSoft\ABMGeneratorBundle\DependencyInjection;

use Symfony\Component\Config\Definition\Builder\TreeBuilder;
use Symfony\Component\Config\Definition\ConfigurationInterface;

/**
 * This is the class that validates and merges configuration from your app/config files.
 *
 * To learn more see {@link http://symfony.com/doc/current/cookbook/bundles/configuration.html}
 */
class Configuration implements ConfigurationInterface
{
    /**
     * {@inheritdoc}
     */
    public function getConfigTreeBuilder()
    {
        $treeBuilder = new TreeBuilder();
        $rootNode = $treeBuilder->root('abm_generator');

        $rootNode
            ->children()
                ->arrayNode('plantillas')
                    ->isRequired()
                    ->info('Debe definir un bloque que contendra al CRUD el que usted definio en su layout principal, ej. content, contenido o page_content.')
                    ->example(array("abm_generator" => array("plantillas" =>array("block_content" => "page_content"))))

                    ->children()
                        //->scalarNode('block_content')->defaultValue('page_content')->end()
                        ->scalarNode('block_content')->info('Debe definir un bloque que contendra al CRUD el que usted definio en su layout principal, ej. content, contenido o page_content.')->isRequired()->cannotBeEmpty()->end()
                        ->scalarNode('layout_gral')->defaultValue('@ABMGenerator/layout.html.twig')->end()
                        ->scalarNode('box_crud')->defaultValue('@ABMGenerator/box_crud.html.twig')->end()
                        ->scalarNode('box_modal')->defaultValue('@ABMGenerator/modal.html.twig')->end()
                    ->end()
                ->end()
            ->end();
        $rootNode
            ->children()
                ->arrayNode('index')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode("botones_arriba")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('nuevo')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Nuevo')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-plus')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('filtro')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Buscar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-filter')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-success btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('paginador')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('')->end()
                                        ->scalarNode('icon')->defaultValue('')->end()
                                        ->scalarNode('class')->defaultValue('pagination pagination-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode("botones_abajo")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('nuevo')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Nuevo')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-plus')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('filtro')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Buscar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-filter')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-success btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('paginador')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('')->end()
                                        ->scalarNode('icon')->defaultValue('')->end()
                                        ->scalarNode('class')->defaultValue('pagination pagination-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
            ->end();

        $rootNode
            ->children()
                ->arrayNode('new')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode("botones_arriba")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('guardar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('guardar_nuevo')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar y Nuevo')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode("botones_abajo")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('guardar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('guardar_nuevo')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar y Nuevo')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()

                    ->end()
                ->end()
            ->end();

        $rootNode
            ->children()
                ->arrayNode('edit')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode("botones_arriba")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('guardar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('borrar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Borrar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-trash')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-danger btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode("botones_abajo")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('guardar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Guardar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-floppy-o')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('borrar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Borrar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-trash')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-danger btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()

                    ->end()
                ->end()
            ->end();

        $rootNode
            ->children()
                ->arrayNode('show')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode("botones_arriba")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('editar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Editar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-pencil')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('borrar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Borrar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-trash')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-danger btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode("botones_abajo")
                            ->canBeDisabled()
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('editar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Editar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-pencil')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-primary btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('borrar')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Borrar')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-trash')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-danger btn-sm')->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ir_lista')
                                    ->canBeDisabled()
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('label')->defaultValue('Ir a la Lista')->end()
                                        ->scalarNode('icon')->defaultValue('fa fa-list')->end()
                                        ->scalarNode('class')->defaultValue('btn btn-default btn-sm')->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()

                    ->end()
                ->end()
            ->end();



        return $treeBuilder;
    }
}
